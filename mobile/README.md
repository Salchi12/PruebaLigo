# Mobile setup

Download a published My Demo App Android APK and set `APP_PATH=/absolute/path/app.apk`. Start an Android emulator, install dependencies and run `npm run test:smoke`.

Accessibility IDs can differ by APK release. Keep selectors inside page objects only; update the page objects for the exact release used by the submission.
