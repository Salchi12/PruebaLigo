import {LoginPage} from '../../src/pages/LoginPage'; import {ProductsPage} from '../../src/pages/ProductsPage'; import {CartPage} from '../../src/pages/CartPage';
describe('Mobile critical flow',()=>{it('MOB-01 valid login @mobile @p0 @smoke @regression',async()=>{const l=new LoginPage();const p=new ProductsPage();await l.login(process.env.MOBILE_USERNAME||'bob@example.com',process.env.MOBILE_PASSWORD||'10203040');await expect(p.screen).toBeDisplayed();});it('MOB-04 open cart @mobile @p0 @smoke @regression',async()=>{const p=new ProductsPage();const c=new CartPage();await p.cart.click();await expect(c.checkout).toBeDisplayed();});});
